loop: 144
title: runtime frame components
status: completed
summary:
  - Split the runtime shell into dedicated RuntimeHeader and RuntimeBody components.
  - Added a header view contract with explicit layout labeling for future focus-mode transitions.
  - Kept the top-level Dioxus app as thin composition over the runtime model.
files:
  - impulse-supervisor/src/runtime.rs
verification:
  - cargo test --workspace -p impulse-supervisor
  - cargo clippy -p impulse-supervisor -- -D warnings
  - cargo fmt --check -p impulse-supervisor
