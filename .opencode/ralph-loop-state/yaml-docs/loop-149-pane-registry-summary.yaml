loop: 149
title: pane registry summary component
status: completed
summary:
  - Added a PaneRegistrySummary component backed by a dedicated summary view model.
  - Lifted supervisor presence, worker count, and project count into RuntimeModel so the runtime shell can react to pane registry changes cleanly.
  - Extended runtime tests to lock summary formatting and state-derived counts.
files:
  - impulse-supervisor/src/runtime.rs
verification:
  - cargo test --workspace -p impulse-supervisor
  - cargo clippy -p impulse-supervisor -- -D warnings
  - cargo fmt --check -p impulse-supervisor
