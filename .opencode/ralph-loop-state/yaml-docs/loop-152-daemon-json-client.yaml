loop: 152
title: daemon json client
status: completed
summary:
  - Added a raw JSON-line daemon client mirror inside daemon_bridge.rs for ping and status requests.
  - Verified the socket protocol against mock Unix socket servers using tokio tests.
  - Kept the client internal and documented the staged dead-code surface for the upcoming facade loops.
files:
  - impulse-supervisor/src/daemon_bridge.rs
verification:
  - cargo test -p impulse-supervisor -- --test-threads=1
  - cargo clippy -p impulse-supervisor -- -D warnings
  - cargo fmt --check -p impulse-supervisor
notes:
  - Workspace-wide loop verification remains blocked by foreign daemon integration failures recorded at loop 150.
