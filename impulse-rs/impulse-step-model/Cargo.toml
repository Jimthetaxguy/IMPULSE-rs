[package]
name = "impulse-step-model"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"
description = "Pure per-step model policy. No HTTP, TUI, store, or provider failover."
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
